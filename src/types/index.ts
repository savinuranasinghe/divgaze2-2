export interface TeamMember {
  name: string;
  role: string;
  photo: string;
  quote: string;
  bio: string;
}

export interface ServiceItem {
  category: string;
  title: string;
  description: string;
}

export interface ProcessStep {
  number: string;
  title: string;
  description: string;
}